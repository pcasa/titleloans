<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag "application", "jquery-ui", 'jquery.fancybox', 'formtastic' %>
    <%= stylesheet_link_tag 'print', :media => "print"%>
    <%= javascript_include_tag :defaults, "jquery-ui-timepicker" %>
    
    <%= yield(:head) %>

	<%= csrf_meta_tag %>
  </head>
  <body>
	<div id="header_container">
		<div id="header">
		<div id="logo">Atlanta Title Solutions</div>
		<%= render :partial => "layouts/usernav" %>
			<%= render :partial => "layouts/menu" %>

		</div>
	</div>
	<div class="clear"></div>
    <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
	  
	 	
      <%= content_tag :h1, yield(:title) if show_title? %>

		
      <%= yield %>
    </div>
	<%= yield(:scripts) %>
  </body>
</html>
