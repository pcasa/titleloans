<%= div_for(photo, :class => "photo") do %>
	<%= image_tag photo.image_url(:thumb) if photo.image? %>
	<%= link_to "Delete", [current_company, @title_loan, photo], :method => :delete, :confirm => "Are you sure?", :remote => :true %>
<% end %>